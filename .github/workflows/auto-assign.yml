name: Assign issues to Jezza34000

on:
  issues:
    types:
      - opened
      - reopened

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign issue to Jezza34000
        uses: actions-ecosystem/action-add-assignees@a5b84af721c4a621eb9c7a4a95ec20a90d0b88e9 # Version 1.0.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          assignees: Jezza34000
